nav.Navigation
    .Navigation-helper
        navigation-item.Navigation-item(
            title='{{ item.name }}'
            state='{{ item.sref }}'
            icon='{{ item.icon }}'
            action='item.action'
            ng-repeat='item in items track by $id(item)'
        )
