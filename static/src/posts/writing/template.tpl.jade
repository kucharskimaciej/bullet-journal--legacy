h1 add new post
form(   name='newPostForm'
        ng-submit='ctrl.onSubmit(newPostForm.$valid, ctrl.vm.post)'
        novalidate )

    ui-form-field(type='select' model='ctrl.vm.post.type' label='Type' options='["regular", "daily"]')
    ui-form-field(type='text' label='Post title' model='ctrl.vm.post.title')
    ui-form-field(type='textarea' model='ctrl.vm.post.original_content')
    button(type='submit') submit
