ng-switch(on='type')
    div.form-group(ng-switch-when='text' ui-form-validation='{{ validates }}')
        input(type='{{ type }}' ng-model="$parent.model" class="form-control" placeholder="{{ label }}")

    div.field(ng-switch-when='textarea' ui-form-validation='{{ validates }}')
        textarea(ng-model='$parent.model' class="form-control" placeholder="{{ label }}")

    div.field(ng-switch-when='select' ui-form-validation='{{ validates }}')
        label(ng-if='label') {{ label }}

        .btn-group(dropdown='' on-toggle='toggled(open)')
            button.btn.btn-sm.btn-primary.dropdown-toggle(type='button' dropdown-toggle='')
                | {{ $parent.model  || 'Default' }}
                span.caret
            ul.dropdown-menu(role='menu')
                li(ng-repeat='option in options')
                    a(ng-click='toggle(option)') {{ option }}

            input(type='hidden' ng-model='$parent.model')
